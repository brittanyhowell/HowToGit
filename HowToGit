# Making a folder a repository
	. Online: Make repository
	. Command line: Git init
	. git remote add origin <link to online repository>

# Adding files
	. git add <filename>
	. git add *

# Committing changes
	. git commit -m “Commit message” 

# Pushing changes to the server
	. git push origin <branch> 
		eg master

# To add a new branch, write: 
	. git checkout -b <branchName>

# Push the branch to the remote repository
	. git push origin <branchName> 

# Switch back to master branch:
	. git checkout master

# Delete a branch
	. git branch -d <branchName>

# Merging changes
	. Once online, click the pull request
	. Merge pull request
	. Confirm merge
	. Delete branch


 
